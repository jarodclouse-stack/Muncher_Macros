import React from 'react';

/**
 * BackgroundGlow is now handled by the index.css body::after architecture 
 * to ensure maximum stability and zero occlusion across all themes.
 */
export const BackgroundGlow: React.FC = () => {
  return null;
};
